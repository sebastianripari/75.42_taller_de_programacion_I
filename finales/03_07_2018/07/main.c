/*
Cual es el uso de la funcion listen?
Que parametros tiene y para que sirven?
*/

/*
int listen(int skt_fd, int max_standby_conn);
La funcion listen marca al socket como pasivo, 
esto es, un socket que sirve para aceptar conexiones
entrantes. 
// Parametros:
** skt_fd **: Socket que va a funcionar de forma pasiva.
** max_standby_conn**: Maxima cantidad de conexiones 
                       en espera, por ser aceptadas.
*/

int main() {
	return 0;
}